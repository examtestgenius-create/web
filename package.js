
const CFG=window.STUDYHUB_CONFIG||{};const sku=new URL(location.href).searchParams.get('sku')||'';const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>m==='&'?'&amp;':m==='<'?'&lt;':m==='>'?'&gt;':m==='"'?'&quot;':'&#39;');const money=c=>Number(c)>0?new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR'}).format(Number(c)/100):'Price not set';async function cat(){for(const u of [CFG.liveCatalogUrl,CFG.fallbackCatalogUrl].filter(Boolean)){try{const r=await fetch(u,{cache:'no-store'});if(r.ok)return await r.json()}catch(e){}}throw Error('Catalog unavailable')}function norm(x){return{sku:x.sku,title:x.title||x.sku,type:x.bundle_type,grade:x.grade,subject:x.subject_or_all,year:x.year_or_range,price:x.price_cents,files:x.file_count,desc:x.description||''}}async function init(){try{const p=await cat();const i=(p.items||[]).map(norm).find(x=>x.sku===sku);if(!i)throw Error('Bundle not found');$('detailStatus').innerHTML=`<span class="eyebrow">${esc(i.type)}</span><h1>${esc(i.title)}</h1><p class="muted">${esc(i.sku)}</p>`;$('packageDetailRoot').innerHTML=`<section class="card"><h2>What you get</h2><p>${esc(i.desc).replace(/\n/g,'<br>')}</p><ul><li>Grade ${esc(i.grade)}</li><li>${esc(i.subject)} • ${esc(i.year)}</li><li>${Number(i.files||0)} files counted</li><li>ZIP delivery and invoice after PayFast confirmation</li></ul><div class="card-bottom"><strong class="price">${money(i.price)}</strong><a class="btn btn-primary" href="checkout.html?sku=${encodeURIComponent(i.sku)}">Buy now</a></div></section>`}catch(e){$('detailStatus').innerHTML=`<span class="eyebrow">Bundle</span><h1>Unavailable</h1><p>${esc(e.message)}</p>`}}init();
