STUDYHUB GO-LIVE (TODAY)

1) Upload all files (except backend_appsscript/) to your GitHub repo root and enable GitHub Pages.

2) In config.js set:
   - webappUrl = your Apps Script Web App URL (ends with /exec)
   - siteBaseUrl = your GitHub Pages URL

3) Apps Script:
   - Ensure Script Properties are set (SITE_BASE_URL, SHEET_ID, NOTIFY_EMAIL, PAYFAST keys, DRIVE_ROOT IDs)
   - Deploy Web App: Execute as Me, Access: Anyone

4) Test payment:
   - Buy a bundle -> PayFast -> return to payment-success.html?order_id=...
   - Confirm delivery link appears

NOTE: backend_appsscript/*.txt are reference exports. Use your working Apps Script project if already deployed.
